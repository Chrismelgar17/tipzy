# Base URL for the integrated Hono API
# Use your dev server origin (LAN IP or tunnel)
# Examples:
# - Local web dev: http://localhost:8081
# - LAN (Android/iOS on same Wi-Fi): http://192.168.1.100:8081
# - Tunnel (Expo with ngrok): https://<your-tunnel-subdomain>.ngrok.app
EXPO_PUBLIC_RORK_API_BASE_URL=auto
# Preferred alias used by axios + tRPC URL resolution
EXPO_PUBLIC_API_URL=auto
# To force a backend tunnel (e.g., ngrok http 3001), set both to that URL:
# EXPO_PUBLIC_RORK_API_BASE_URL=https://your-backend-tunnel.example
# EXPO_PUBLIC_API_URL=https://your-backend-tunnel.example

# Backend configuration
DATABASE_URL=postgres://postgres:postgres@db:5432/tipzy
ADMIN_PASSWORD=admin123
JWT_SECRET=change-me-access
REFRESH_SECRET=change-me-refresh
PORT=3001
HOST=0.0.0.0

# SMTP settings (fill these with real credentials)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tipzy.team@gmail.com
SMTP_PASS=TipzyApp24
MAIL_FROM=tipzy.team@gmail.com
MAIL_FROM_NAME=Tipzy

# OAuth (frontend Expo)
EXPO_PUBLIC_GOOGLE_EXPO_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=

# OAuth (backend verification)
# Comma-separated list of allowed Google OAuth client IDs
GOOGLE_CLIENT_IDS=
# Comma-separated audience list for Apple identity tokens
# Usually include your iOS bundle id and/or Services ID
APPLE_AUDIENCES=
